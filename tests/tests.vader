Given (Simple text):
  This text contain errors.

Execute (Nothing):
  LanguageToolSetUp
  sleep 10
  LanguageToolCheck
  sleep 10

Expect (Nothing changed):
  This text contain errors.

Then (There is errors):
  Assert exists('b:errors')
  Assert !empty(b:errors)
